# 코드 구조

2026-09-16 · 기능과 통신 번호를 유지한 구조 정리

## 경계와 책임

| 영역 | 파일 | 책임 |
|---|---|---|
| 통신 상수 | include/ProtocolLimits.hpp | 헤더 오프셋·크기, 최대 패킷/본문/채팅 크기, 방 정원 |
| 실행 설정 | include/NetworkSettings.hpp, ServerLimits.hpp, client/ClientSettings.hpp | 기본 포트·연결 수·버퍼·송신 큐, 이벤트 큐, 클라이언트 타임아웃·입력 주기·명령 안내 |
| 연결 자료형 | include/ConnectionKey.hpp, NetworkIo.hpp | 슬롯/세대 키와 Windows 비동기 I/O 컨텍스트 분리 |
| 통신 자료형 | include/MessageCode.hpp, ErrorCode.hpp, PacketHeader.hpp, Packet.hpp | 메시지/오류 코드, 헤더 읽기, 소유 본문과 패킷 직렬화 |
| 게임 자료형 | include/PlayerSession.hpp, RoomSession.hpp | 입장 상태, 방 소속, 정원과 참가자 ID |
| 이벤트/송신 자료형 | include/ServerEvent.hpp, OutboundPacket.hpp | 네트워크에서 받은 이벤트, 수신 대상과 소유 패킷 |
| 네트워크 | include/ClientConnection.hpp, IOCPServer.hpp | 소켓·I/O 수명, 세대 검증, 송신 순서 |
| 서버 연결 | include/ZPDServer.hpp | 네트워크 콜백을 패킷 처리기에 연결 |
| 큐·프레이밍 | include/PacketHandler.hpp, src/PacketHandler.cpp | 수신 조립, 이벤트 큐·로직 스레드, 송신 및 실패 연결 정리 |
| 게임 상태 | include/GameWorld.hpp, src/GameWorld.cpp | 연결별 세션·플레이어 ID 조회, 요청 분기·개발용 입장·Echo |
| 방·채팅 처리 | src/RoomRequests.cpp, ChatRequests.cpp | 방 소속의 일관성, 입·퇴장 알림, 방 전체 채팅 |
| Protobuf 공통 처리 | include/ProtobufCodec.hpp | 본문 파싱·크기 제한과 직렬화 |
| 클라이언트 실행 | client/main.cpp, ClientApplication.cpp, ClientResources.hpp | 실행 인자, 연결 생성, 콘솔·소켓 수명 |
| 클라이언트 처리 | client/RoomChatClient.hpp/.cpp, RoomChatCommands.cpp, RoomChatReceiver.cpp | 송수신 수명, 명령 생성, 응답·알림 처리 |
| 입력·대기 요청 | client/ConsoleInput.cpp, PendingRequest.hpp, EchoRequestBuilder.cpp | 중단 가능한 Unicode 입력, 요청 추적, Echo 분할 |
| 통신 스키마 | proto/echo.proto, session.proto, room.proto, chat.proto | 기능별 Protobuf 정의. 코드·필드 번호는 유지 |
| 테스트 | tests/server_tests.cpp, chat_client_tests.py | 서버 통합/세대/실패 시나리오, 실제 클라이언트 프로세스 검증 |

`GameWorld`는 Windows API나 스레드·송신 콜백을 사용하지 않습니다. 게임 로직은 수신 대상과 패킷의 목록을 반환하고 `PacketHandler`가 전송합니다. 게임 상태는 로직 스레드만 수정하고 종료 시 join 후 초기화합니다. 큐 잠금을 잡은 채 게임 요청을 실행하거나 송신하지 않습니다.

CMake의 `server_core`와 `chat_client`가 구현을 각각 한 번 컴파일합니다. `zpd-server`, `zpd-client`, `zpd-server-tests`는 필요한 라이브러리를 연결합니다. 기존 테스트 실행 파일의 대화형 진입은 호환을 위해 유지했습니다.

## 이름 변경

| 이전 | 현재 | 이유 |
|---|---|---|
| Define.hpp | ConnectionKey.hpp / NetworkIo.hpp / NetworkSettings.hpp | 식별자·I/O·설정을 분리 |
| PacketCode.hpp | MessageCode.hpp / ErrorCode.hpp | 통신 방향 코드와 오류 의미를 분리 |
| ClientInfo | ClientConnection | 단순 정보가 아닌 소켓/I/O 수명 관리 객체 |
| ConnectionKey.clientId | ConnectionKey.slotIndex | 플레이어 ID가 아닌 재사용 슬롯 |
| GameSession | RoomSession | 현재 모델의 실제 역할은 방 |
| PlayerState::Connected | PlayerState::AwaitingEntry | 개발용 입장 전 상태임을 명확히 표시 |
| PacketHandlerEvent | ServerEvent | 연결·수신·종료 이벤트의 공통 자료형 |
| Delivery.connection | OutboundPacket.recipient | 목적지와 송신 패킷임을 명시 |
| PacketHandler.Run / Handle | Start / ReceiveBytes | 시작과 원시 수신 바이트 접수 구분 |
| RunEchoClient / InteractiveClient | RunChatClient / RoomChatClient | 방·채팅 클라이언트 역할 반영 |
| Command / Process / Snapshot | HandleInputLine / HandleServerPacket / ApplyRoomSnapshot | 입력·처리 대상과 동작 명시 |

## 검증 실행

기존 실행 파일을 건드리지 않도록 이번 검증은 `out/build/refactor-debug`에서 수행합니다. 일반 개발에서는 기존 CMake preset을 그대로 사용할 수 있습니다.

```powershell
cmake --build out/build/refactor-debug --config Debug
ctest --test-dir out/build/refactor-debug -C Debug --output-on-failure
python -X utf8 tests/chat_client_tests.py out/build/refactor-debug/Debug
```

이 폴더의 서버는 `Debug/zpd-server.exe`, 전용 클라이언트는 `Debug/zpd-client.exe`입니다.
